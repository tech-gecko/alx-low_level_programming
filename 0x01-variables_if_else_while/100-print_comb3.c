#include <stdio.h>

int main(void)
{
	int i;
	int j;

	for (i = 0; i <= 9; i++)
	{
		for (j = i + 1; j <= 9; j++)
		{
			putchar(i + '0');
			putchar(j + '0');
			if (a == 8 && b == 9)
				break;
			putchar(',');
			putchat(' ');
		}
	}
	putchar('\n');
}
