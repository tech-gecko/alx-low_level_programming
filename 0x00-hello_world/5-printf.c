#include <stdio.h>

/**
 * main - prints a string using format specifier %s
 *
 * Return: NULL
 */
int main(void)
{
	printf("%s\n", "with proper grammar, but the outcome is a piece of art,");
	return (0);
}
