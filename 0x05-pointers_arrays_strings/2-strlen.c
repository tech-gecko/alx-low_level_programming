#include "main.h"
#include <string.h>

/**
 * _strlen - Returns the length of a string
 * @s: String in question
 *
 * Return: Always 0 (success)
 */
int _strlen(char *s)
{
	char *temp;
	temp = s;
	strlen(temp);
	return (0);
}
