#include "main.h"

/**
 * more_numbers - Prints 0 to 14 (10 times), followed by a new line.
 *
 *Return: Nothing.
 */
void more_numbers(void)
{
	for (i = 0; i <= 9; i++)
	{
		for (i = 0; i <= 14; i++)
		{
			_putchar(i);
		}
		_putchar('\n');
	}
}
